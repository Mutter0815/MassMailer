run:
  timeout: 5m
  tests: false
linters:
  disable-all: true
  enable:
    - govet
    - gofmt
    - errcheck
issues:
  exclude-use-default: true
  exclude-rules:
    - path: ^deployments/
    - path: ^migrations/

